{
  "entities": {
    "users": {
      "title": "User",
      "description": "User profile and balances",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "email": { "type": "string" },
        "role": { "type": "string" }
      },
      "required": ["id", "email"]
    },
    "matches": {
      "title": "Match",
      "description": "Match entries",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" }
      },
      "required": ["id", "title"]
    }
  }
}
